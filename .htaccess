<IfModule mod_rewrite.c>
RewriteEngine On

# Redirect requests like /index to /index.php
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^([^/]+)/?$ $1.php [L]

# Optional: make index.php load automatically
DirectoryIndex index.php
</IfModule>
